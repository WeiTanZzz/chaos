PORT=4400
DATABASE_PATH=api.sqlite
# Print capability spans to the console. A real deployment swaps the exporter in src/tracing.ts.
TRACING=false
# Which surfaces this deployment serves. Unset means all of them.
# SURFACES=http,openapi
# SURFACES=mcp
